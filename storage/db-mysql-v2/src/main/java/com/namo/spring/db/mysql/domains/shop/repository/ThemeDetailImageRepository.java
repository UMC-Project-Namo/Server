package com.namo.spring.db.mysql.domains.shop.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.namo.spring.db.mysql.domains.shop.entity.ThemeDetailImage;

public interface ThemeDetailImageRepository extends JpaRepository<ThemeDetailImage, Long> {
}
